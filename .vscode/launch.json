{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Listen for Xdebug",
            "type": "chrome",
            "request": "launch",
            "port": 9003,
            "pathMapping": {
                "/var/www/html/wp-content/plugins/local-dev-plugin": "${workspaceFolder}"
            }
        }
    ]
}